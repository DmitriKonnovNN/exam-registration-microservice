server:
  port: 8081
spring:
  application:
    name: fraudcheck
  datasource:
    password: P@ssw0rd
    url: jdbc:postgresql://localhost:5432/fraud
    username: dmitrikonnov
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true